{
  "name": "@loyal/yield-routing-actions",
  "version": "0.1.0",
  "type": "module",
  "description": "Unsigned Loyal yield-route action instruction builders.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run generate:abi && tsc -p tsconfig.build.json",
    "generate:abi": "bun ./scripts/generate-loyal-hub-abi.ts",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5"
  }
}
