name: operator-tools-image

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish-operator-tools:
    name: Build once and publish operator tools
    uses: ./.github/workflows/rust-image-build.yml
    with:
      images: operator-tools
      publish: true
    secrets: inherit
